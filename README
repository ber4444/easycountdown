As New Year's Eve is coming, here is a small OS X app that counts down to midnight.

From the menu, you can make it fullscreen (Cmd+Q to exit), as well as test the "midnight effect" (fireworks simulation and playing whatever video you defined in Settings).
